# 实施任务：会话级执行状态入口与工作台冷启动优化

> 对应 Change: `update-session-workbench-entry-and-loading`

## 1. 交互语义调整

- [x] 1.1 将“查看执行状态”主入口迁移到聊天页会话头部
- [x] 1.2 移除或下线消息动作区中的执行状态主入口
- [x] 1.3 为会话头部入口定义 idle、running、error、warming 四种状态表达
- [x] 1.4 让欢迎态与已有消息态共用同一套会话级入口语义

## 2. 像素语义入口

- [x] 2.1 设计并实现“像素工位徽章”入口样式
- [x] 2.2 保证该入口在 dark / light 双主题下均保持清晰可辨
- [x] 2.3 控制入口动效，只在运行态或 warming 态提供轻量反馈
- [x] 2.4 补充视觉系统文档，说明该入口的边界与复用方式

## 3. 工作台首开过渡

- [x] 3.1 为沉浸式工作台增加加载封面，承接首次冷启动
- [x] 3.2 在 iframe ready 后平滑切换到真实办公室画面
- [x] 3.3 明确处理 iframe 加载失败或超时场景，避免再次退化为裸白页
- [x] 3.4 保持工作台外层控件极简，不因加载封面重新挤占 iframe 主舞台

## 4. 预热与缓存

- [x] 4.1 为当前活跃会话增加静默预热能力
- [x] 4.2 将工作台缓存升级为 `LRU=2`
- [x] 4.3 定义刷新、切换会话与淘汰旧实例时的状态更新策略
- [x] 4.4 验证两个会话来回切换时能复用已有工作台实例

## 5. 路由与恢复入口

- [x] 5.1 保留 `/visualize/:sessionId` 作为深链接与宿主同步层
- [x] 5.2 优化从聊天页进入工作台的主路径，避免用户感知为“跳到空页面”
- [x] 5.3 调整返回后的轻量恢复提示，使其成为辅助入口而非主入口

## 6. 测试与文档

- [x] 6.1 为会话级入口、warming 状态和消息级入口下线补充单元测试
- [x] 6.2 为工作台加载封面、iframe ready 切换和 `LRU=2` 缓存补充测试
- [x] 6.3 更新阶段六文档与 UI 视觉系统指南
- [x] 6.4 运行 `openspec validate update-session-workbench-entry-and-loading --strict`
